bool findPattern(string p, string s)
{
    int n=s.length();
    int m=p.length();
    for(int i=0;i<n-m+1;i++){
        string ans=s.substr(i,m);
        if(ans==p){
            return 1;
        }
    }
    return 0;
}
